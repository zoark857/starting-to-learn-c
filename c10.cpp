#include <stdio.h>
#include <math.h>

int main() {

    // some basic math function

    float x = 3.12;


    // x = sqrt(x);
    // x = pow(x, 2);
    // x = round(x);
    // x = floor(x);
    // x = ceil(x);
    // x = abs(x);
    // x = log(x);
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);



    printf("%f", x);


    return 0;

}